# apt_pgp_key_url: "https://packages.fluentbit.io/fluentbit.key"
fluentbit_apt_repo_url: "deb https://packages.fluentbit.io/{{ ansible_distribution|lower }}/{{ ansible_distribution_release|lower }} {{ ansible_distribution_release|lower }} main"
_fluentbit_require_package:
  - apt-transport-https
